/** The welcome/subtitle section at the top of the dashboard. */
export function DriveSearchSection({
  query: _query,
  onQueryChange: _onQueryChange,
}: {
  query: string
  onQueryChange: (value: string) => void
}): JSX.Element {
  return <></>
}